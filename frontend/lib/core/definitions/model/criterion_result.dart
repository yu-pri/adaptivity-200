import 'package:adaptivity_200/core/definitions/model/criterion.dart';

class CriterionResult {
  final Criterion criterion;
  final int tier;
  final int score;
  CriterionResult(this.criterion, this.score, this.tier);
}